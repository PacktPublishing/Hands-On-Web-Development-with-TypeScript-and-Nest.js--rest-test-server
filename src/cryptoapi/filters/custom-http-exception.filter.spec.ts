import { CustomHttpExceptionFilter } from './custom-http-exception.filter';

describe('CustomHttpExceptionFilter', () => {
  it('should be defined', () => {
    expect(new CustomHttpExceptionFilter()).toBeDefined();
  });
});
